[package]
name = "mobile_tests"
version = "0.1.0"
edition = "2021"

[dependencies]
askar-crypto = "0.3.1"
p256 = "0.13.2"
secure-env = { path = "../.." }
uuid = { version = "1.7.0", features = ["v4", "fast-rng"] }
